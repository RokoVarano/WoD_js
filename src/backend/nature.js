class Nature {
  constructor(
    name,
    description,
    system,
    source,
  ) {
    this.name = name;
    this.description = description;
    this.system = system;
    this.source = source;
  }
}

export default Nature;