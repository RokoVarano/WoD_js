class Merits {
  constructor(
    cost,
    description,
    system,
    source,
  ) {
    this.cost = cost;
    this.description = description;
    this.system = system;
    this.source = source;
  }
}

export default Merits;