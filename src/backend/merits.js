class Merit {
  constructor(
    name,
    cost,
    description,
    source,
  ) {
    this.name = name;
    this.cost = cost;
    this.description = description;
    this.source = source;
  }
}

export default Merit;
